# SOAP endpoints (configurable per deployment)
# These should point to the WSDL URLs exposed by servidor_aplicacion
# For local testing the servidor_aplicacion publishes SOAP on HTTP (no SSL)
soap.auth.wsdl=http://localhost:8081/authservice?wsdl
soap.file.wsdl=http://localhost:8080/fileservice?wsdl

server.port=8082

server.port=8082

# If you run servidor_aplicacion on another host, replace 'localhost' above with that host or set via env variables.
